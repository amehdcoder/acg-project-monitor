## Goal\n\nAdd a new **SEITRF Model (NTDs)** as a Quick Start preset on the Math Modeling page (next to SEITF Model (NTD)), encode its 21-compartment governing equations with MDA-as-pulse semantics for the **Treated** compartments (`T_hce`, `T_hae`) on **both** SEITF and SEITRF, and make **Run Simulation** auto-run the 5 Research-Question analyses whenever either model is active.\n\n## What changes\n\n### 1. New preset: `SEITRF Model (NTDs)` — `src/components/MathModelingView.tsx` (PRESET_MODELS array, after SEITF)\n\nAdds a 21-compartment preset with:\n\n- New compartments `Rhce`, `Rhae` (recovered ever-treated SAC / adults)\n- Force-of-infection notation `lambda_sac = beta_sac * epsilon * Fc * kappa_w`, `lambda_adult` analogous, `lambda_s = beta_s * epsilon * Fm * kappa_w`\n- New continuous parameters `sigma_sac`, `chi_sac`, `sigma_adult`, `chi_adult`, `phi_c`, plus reuse of existing ones\n- Equations exactly mirroring the LaTeX system (Shcn, Ehcn, Ihcn, Shce, Ehce, Ihce, Thce, Rhce, Shan, Ehan, Ihan, Shae, Ehae, Ihae, Thae, Rhae, Fm, Fc, Ss, Es, Is)\n- Sensible default initial values (`Shcn(0)>0`, `Ss(0)>0`, others ≥ 0)\n\nA new Quick Start button appears automatically because it just maps over `PRESET_MODELS`.\n\n### 